server:
  port: 9002

spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848 #nacos的配置地址
management:
  endpoints:
    web:
      exposure:
        include: "*"